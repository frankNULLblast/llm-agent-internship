# 支线一：FastAPI（接口层）

将主线 Agent 封装为 HTTP 接口，让网页 / 程序可以调用。5 天渐进构建 `patent-api` 项目：

| 天 | 主题 | 对应内容 |
|---|---|---|
| day01 | HTTP服务 | 项目脚手架、FastAPI 启动（`main.py`） |
| day02 | 请求响应 | 路由与请求 / 响应模型 |
| day03 | 复核分流 | 费用计算与复核逻辑（`fee_calc.py`） |
| day04 | 测试契约 | 接口测试与契约（`test_main.py` / `API_CONTRACT.md`） |
| day05 | 发布复盘 | 发布清单与复盘（`RELEASE_CHECKLIST.md`） |

完整代码见 `patent-api/`，运行方式见其 `README.md`。
