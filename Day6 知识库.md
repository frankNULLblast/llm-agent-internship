# Day 6 知识库（MOC）

> 本周专题日：把"写一个小工具"升级成"走完软件开发全过程"。核心是**顺序**——先文档和测试样例，后编码和发布。

## 今日主线
- 六阶段：需求 → 设计 → 编码 → 集成 → 测试 → 上线，每阶段都有可检查证据。
- 成果：专利费用批次计算 CLI（`fee_calc.py`），金额用 `Decimal`、失败不写半截输出、可复现发布 `v0.1.0`。

## 知识点索引
- [[软件开发六阶段]] — 不是瀑布，每次变化重查受影响阶段
- [[金额用 Decimal 不用 float]] — 二进制浮点的不可见误差
- [[原子写文件避免半截输出]] — 临时文件 + replace 的失败安全

## 关联回顾
- Day 4「业务规则只存在一处」：[[response_format 与 JSON 校验]]（响应侧自己校验）
- Day 5「模型抽·程序核·人拍板」：[[模型与程序的责任边界]]
- 下一日：Day 7 Codex 与 WorkBuddy 协作

## 今日一句话
先写"算什么、怎么算、错怎么办"的文档和样例，再写代码；最后用固定版本让别人能复现——这才是工程，不是脚本。
