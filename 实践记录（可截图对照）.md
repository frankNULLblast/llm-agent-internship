# Day 6 实践记录（可截图对照）

> 纯 CLI 任务，下面每一步都可复制进 PowerShell 运行并截图。运行前先激活 venv：`.venv\Scripts\Activate.ps1`（或全程用 `$env:PY="C:\Users\rog 16\llm-agent-internship\.venv\Scripts\python.exe"` 代替 `python`）。
> 工作目录：`llm-agent-internship\software-process\day06\fee-calculator`

## 0. 进目录 + 激活环境
```powershell
cd "$HOME\llm-agent-internship\software-process\day06\fee-calculator"
.\.venv\Scripts\Activate.ps1
```
截图点：命令行出现 `(.venv)` 前缀。

## 1. 编译（语法检查）
```powershell
python -m py_compile fee_calc.py test_fee_calc.py
```
截图点：无报错即通过（无输出=成功）。

## 2. 跑 9 个固定测试
```powershell
python -m unittest -v
```
截图点：最后一行 `Ran 9 tests ... OK`（9 个 `... ok`）。

## 3. 集成命令生成 summary.json
```powershell
python fee_calc.py fees.json -o summary.json
python -m json.tool --no-ensure-ascii summary.json
```
截图点：`已写入 summary.json` + JSON 里 `total_yuan: "5600.50"`、`by_application` 两个申请号小计。

## 4. 挑战任务：只读 --fee-type 筛选
```powershell
python fee_calc.py fees.json -o summary_demo.json --fee-type 代理服务费
python -m json.tool --no-ensure-ascii summary_demo.json
```
截图点：输出 `record_count: 2`、`by_fee_type` 只有 `代理服务费: "4700.00"`、总额 `4700.00`；默认不传 `--fee-type` 仍是 5600.50。

## 5. 本地发布：克隆复现 + 标签验证
```powershell
# 在 software-process/day06/ 下
git clone fee-calculator fee-calculator-release-check
cd fee-calculator-release-check
python -m unittest -v
python fee_calc.py fees.json -o summary.json
cd ..\fee-calculator
git tag v0.1.0
cd ..\fee-calculator-release-check
git fetch --tags
git checkout v0.1.0
python -m unittest -v
python fee_calc.py fees.json -o summary.json
git describe --tags --exact-match
```
截图点：克隆目录 `Ran 9 tests ... OK`、总额 `5600.50`、`git describe` 返回 `v0.1.0`（detached HEAD 提示属正常）。

## 验收清单自查
- [x] 需求含用户/输入/输出/错误/非目标/固定验收（5600.50）
- [x] 设计说明数据流、三函数接口、失败写文件边界
- [x] 金额全程 `Decimal`，无 `float`
- [x] 9 测试全过；失败不覆盖旧输出
- [x] README 同时给 PowerShell 7 与 Bash 命令
- [x] RELEASE_CHECKLIST 填入实际结果
- [x] 新克隆 `v0.1.0` 可复现
- [x] 能用六阶段证据表解释全过程
