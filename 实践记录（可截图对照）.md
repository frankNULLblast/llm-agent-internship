# Day 5 实践记录（可截图对照）

> 与 `week1/day05/` 仓库文件一一对应，方便边跑边截图。

## 1. 离线测试（不调 API、无费用）
命令（PowerShell，已激活 `.venv`）：
```powershell
cd week1\day05
python -m unittest -v
```
预期：5 个用例 `... ok`，末行 `Ran 5 tests ... OK`。

真实结果（2026-07-17）：
```
test_bad_number_uses_priority_review ... ok
test_extra_field_is_rejected ... ok
test_low_confidence_uses_priority_review ... ok
test_missing_amount_uses_priority_review ... ok
test_valid_record_uses_standard_review ... ok
----------------------------------------------------------------------
Ran 5 tests in 0.000s
OK
```

## 2. 在线真实调用（覆盖模型名）
命令：
```powershell
$env:DEEPSEEK_MODEL="deepseek-chat"
python patent_bill.py patent_bill_ocr.txt -o result.json
python -m json.tool --no-ensure-ascii result.json
```
预期：写入 `result.json`，退出码 0，`route=standard_manual_review`。

真实 `result.json`（节选）：
```json
{
  "record": {
    "document_type": "电子发票",
    "patent_application_number": "CN202410123456.7",
    "invoice_number": "24503100000123456789",
    "issue_date": "2026-07-10",
    "payer": "示例智能科技有限公司",
    "payee": "示例知识产权服务有限公司",
    "service_item": "发明专利申请代理服务费",
    "amount_yuan": 3500.0,
    "confidence": 0.95
  },
  "route": "standard_manual_review",
  "review_reasons": []
}
```

## 3. 固定测试输入变体（确认优先复核）
教程要求：把副本中申请号删掉，或把离线 `confidence` 改为 `0.5`，确认进入 `priority_manual_review`。
- 离线测试已覆盖：`confidence=0.5` → 含「模型置信度低于 0.85」；`patent_application_number="unknown"` → `route=priority_manual_review`。两条均通过。

## 4. 安全核对
- `patent_bill_ocr.txt` 首行标注虚构；全文件无真实企业/个人/票据号。
- `patent_bill.py` 中 `DEEPSEEK_API_KEY` 仅 `os.getenv` 读取，无硬编码。
- `result.json` 只含抽取结果与路由，无 Key、无原图。
