# 实践记录（可截图对照）— Day 8 待办 CLI

> 每个场景给「敲什么命令 + 预期看到什么」，照着跑就能截图当证据。
> Day 8 只用 Python 标准库，无需 venv，任意 Python 3 都能跑。

## 场景 0：进入项目目录

```powershell
Set-Location "$HOME\llm-agent-internship\week2\day08\todo-cli"
python --version
```

截图：命令行显示 Python 版本（证明环境就绪）。

## 场景 1：新增两条待办

```powershell
python todo.py add "买牛奶"
python todo.py add "写周报"
```

截图：两行 `已添加 ID 1` / `已添加 ID 2`。

## 场景 2：列出全部

```powershell
python todo.py list
```

截图：

```
[ ] 1: 买牛奶
[ ] 2: 写周报
```

## 场景 3：标记完成并复查

```powershell
python todo.py done 1
python todo.py list
```

截图：`done 1` 回 `已完成 ID 1`；`list` 变成 `[x] 1: 买牛奶`、`[ ] 2: 写周报`。

## 场景 4：clear-done 挑战（删除所有已完成项）

```powershell
python todo.py clear-done
python todo.py list
```

截图：`已清除 1 个已完成事项`；`list` 只剩 `[ ] 2: 写周报`。

## 场景 5：错误输入要返回非 0 退出码

```powershell
python todo.py done 999; "退出码=$LASTEXITCODE"
python todo.py add ""; "退出码=$LASTEXITCODE"
```

截图：

```
错误：不存在 ID 999
退出码=1
错误：待办内容不能为空
退出码=1
```

（这两条是 SPEC 里"空内容/不存在的 ID 返回中文错误且非 0 退出"的硬指标，必须截到。）

## 场景 6：持久化文件

```powershell
Get-Content todos.json
```

截图：能看到 UTF-8 的 JSON 数组，佐证数据落盘（`todos.json` 已被 .gitignore 忽略，不会进仓库）。

## 场景 7：SPEC 与 git 证据

- 打开 `SPEC.md` 截图：可验收需求清单（add/list/done/delete/clear-done + 错误码 + 只标准库）。
- 终端跑：

```powershell
git branch
git log --oneline -3
```

截图：`* day08` 分支 + 提交 `7b028e1 Day8: 待办 CLI(...)`。

## 验收清单对照

| 验收项 | 对应场景 |
|---|---|
| 五个子命令都可用 | 场景 1~4 |
| clear-done 删除已完成项 | 场景 4 |
| 非法输入报错且退出码非 0 | 场景 5 |
| 数据持久化到 JSON | 场景 6 |
| 可验收需求写在 SPEC + 已提交 git | 场景 7 |
